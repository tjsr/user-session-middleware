import { describe, test } from 'vitest';

describe('getDbUserByEmail', () => {
  test.todo('Should call retrieveUserData if function is set', () => {

  });

  test.todo('Should return a default user object if retrieveUserData is not set', () => {
  });
});
