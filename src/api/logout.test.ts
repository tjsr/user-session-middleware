import { describe, test } from 'vitest';

describe('logout', () => {
  test.todo('Should return a 403 when a user is not currently logged in.', () => {
  
  });

  test.todo('Should call session.save with a HTTP 200 result if we currently have a user.', () => {

  });
}); 
